# Base URL of the HavenWorkspaces ASP.NET Core API.
# Local development uses http://localhost:5122 (see .env.local).
# On Vercel, set this in Project Settings -> Environment Variables to the
# public URL of your deployed API (e.g. https://api.havenworkspaces.com).
NEXT_PUBLIC_API_URL=http://localhost:5122
